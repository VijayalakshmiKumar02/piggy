spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://config-piggy-openshift.app.openshiftcognizant.com/actuator
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
