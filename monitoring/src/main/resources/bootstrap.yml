spring:
  application:
    name: monitoring
  cloud:
    config:
      uri: http://config-piggy-openshift.app.openshiftcognizant.com
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
